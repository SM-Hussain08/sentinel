"""
SENTINEL product release version.

This version identifies the overall SENTINEL platform release.

It is intentionally independent from component-level lineage
identifiers such as:

- Isolation Forest detector version
- feature schema version
- Event Processor runtime version
- incident correlation version
- investigation engine version
- AI prompt/evidence versions

Repository releases follow Semantic Versioning.
"""

SENTINEL_VERSION = "1.0.0"
