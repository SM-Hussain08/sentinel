# ============================================================
# SENTINEL Frontend Tests
# Reproducible Vitest + coverage execution
# ============================================================

FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./

RUN npm ci

COPY . .

ENV NODE_ENV=test

CMD ["npm", "run", "test:coverage"]
